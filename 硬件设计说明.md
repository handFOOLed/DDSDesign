# 硬件设计说明

## 1. 总体结构

```text
                 +--------------------+
UART/KEY/OLED -> | STM32F103C8T6      |
                 |                    |
                 | PB12/PB13/PB15     | -> AD9833 -> 幅度控制/放大 -> GEN OUT
                 | PB8/PB13/PB15      | -> MCP41010 数字电位器
                 | PA0 / ADC1_IN0     | <- SCAN/COL 输入
                 +--------------------+
```

## 2. AD9833 信号源

连接：

- FSYNC -> PB12
- SCLK -> PB13
- SDATA -> PB15
- VCC -> 3.3 V 或模块要求电压
- GND -> 系统地

注意：

- `AD9833_MCLK_HZ` 默认 25 MHz，应与模块晶振一致。
- Sine、Ramp、Square 通过 AD9833 控制寄存器切换。
- AD9833 原始输出幅度较小，后面必须接幅度控制和放大级。

## 3. GEN 幅度控制级

推荐电路：

```text
AD9833 OUT
  -> 100 nF AC 耦合
  -> 二阶低通/重构滤波
  -> MCP41010 可变电阻网络
  -> 高速轨到轨运放
  -> 输出保护电阻 50~100 ohm
  -> GEN OUT
```

MCP41010 连接：

- CS -> PB8
- SCK -> PB13
- SI -> PB15
- VSS -> GND
- VDD -> 3.3 V
- PA/PB/PW 按你的运放增益网络连接

运放建议：

- 供电 3.3 V 或 5 V，输出必须限制在目标接口允许范围。
- 10 MHz、3.3 Vpp 对运放要求较高，压摆率建议不低于 2 * pi * 10 MHz * 1.65 V = 104 V/us，实际留余量可选 150 V/us 以上。
- 若只需要较低频率达到 3.3 Vpp，可以使用较低带宽运放，但 10 MHz 端幅度会下降。

固件当前用线性方式把 `1000..3300 mV` 映射到 MCP41010 `0..255`。实际电路建议标定：

1. 串口输入 `f 1000`、`w sine`。
2. 分别输入 `v 1000`、`v 2000`、`v 3300`。
3. 用示波器测 GEN OUT 实际 Vpp。
4. 如果误差较大，把 `Core/Src/amplitude.c` 中的线性映射改为查表插值。

## 4. SCAN 幅频测试输入

如果被测频率到 10 MHz，PA0 不应直接接高速交流输出，而应接外部检测后的幅度电压：

```text
被测电路输出
  -> 高频峰值检波 / RMS-to-DC / 包络检测
  -> RC 平滑
  -> 限幅保护 0..3.3 V
  -> PA0
```

这样 OLED 上显示的是每个扫频点的幅度包络。固件会按本次扫描的最小/最大幅度动态拉伸 Y 轴，并把低频参考幅度 70.7% 作为 -3 dB 阈值；当曲线跨过阈值时，会在相邻两个频点之间线性插值得到 Fc，并在 OLED 上画出阈值横线和 Fc 竖线。

固件默认配置为 `SCAN_INPUT_USES_ENVELOPE = 1`，也就是把 PA0 当作外部检测器的直流幅度输入。每个频点会等待检测器稳定，然后取 `SCAN_ENVELOPE_SAMPLES` 次 ADC 平均值。若你只做低频测试并希望直接采交流波形，可把 `Core/Inc/app_config.h` 中的 `SCAN_INPUT_USES_ENVELOPE` 改为 `0`。

## 5. COL 输入前端

COL 模式目标是识别 1 Hz 到 100 kHz 的外部波形类型、Vpp 和频率。推荐输入前端：

```text
外部信号
  -> 保护电阻
  -> 可切换增益/衰减
  -> 1.65 V 偏置
  -> 钳位保护
  -> PA0
```

要点：

- PA0 绝不能低于 0 V 或高于 3.3 V。
- 10 mVpp 小信号建议放大，否则 ADC 量化步进约 0.8 mV，噪声会明显影响分类。
- 100 kHz 附近需要足够快的输入缓冲和合理采样窗口。

固件的 COL 模式会自动尝试多个采样间隔，优先选择上升沿数量合适的窗口来计算频率。波形分类不是画屏示波，而是基于采样点分布：方波看高/低电平停留和大跳变，Ramp 看单调斜率比例，剩余近似归为 Sine。`COL_MIN_SPAN_ADC` 约对应 10 mVpp，低于该跨度时容易被噪声影响。

## 6. CubeMX/Keil 使用注意

当前工程为了快速闭环，外设初始化在 `Core/Src/main.c` 的 USER CODE 区域中手写完成，Keil 工程也已手动加入新增源文件。

如果重新用 CubeMX 生成代码：

- 保持 `Keep User Code` 开启。
- 生成后检查 Keil 工程是否仍包含 `ad9833.c`、`amplitude.c`、`ssd1306.c`、`signal_app.c`。
- 检查 `stm32f1xx_hal_conf.h` 是否仍启用 `HAL_ADC_MODULE_ENABLED` 和 `HAL_UART_MODULE_ENABLED`。
- 检查 `startup_stm32f103xb.s` 栈大小是否仍为 `0x800`。
